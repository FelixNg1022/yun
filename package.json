{
  "name": "yun",
  "description": "An iMessage oracle that does the real Chinese numerology math, then interprets it — and tracks whether it was right.",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "@photon-ai/imessage-kit": "^2.1.2",
    "lunar-typescript": "^1.8.6"
  }
}
